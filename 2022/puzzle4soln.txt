cat puzzle4.txt | tr -d '\r' | tr '-' ',' | awk -F ',' '{if ($3 < $1) print $3,$4,$1,$2; else print $1,$2,$3,$4;}' | awk '{if ($3 >= $1 && $4 <= $2 || $3 == $1 && $2 <= $4) print "1"; else print "0"}' | tr '\n' '+' | sed 's/\+$/\n/' | bc -l
cat puzzle4.txt | tr -d '\r' | tr '-' ',' | awk -F ',' '{if ($3 < $1) print $3,$4,$1,$2; else print $1,$2,$3,$4;}' | awk '{if ($3 >= $1 && $3 <= $2 || $4 >= $1 && $4 <= $2) print "1"; else print "0"}' | tr '\n' '+' | sed 's/\+$/\n/' | bc -l
